user  nginx;
worker_processes  auto;

events { worker_connections 1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  tcp_nopush    on;
  tcp_nodelay   on;
  keepalive_timeout  65;

  # gzip for text assets
  gzip on;
  gzip_types text/plain text/css application/javascript application/json application/xml image/svg+xml;
  gzip_min_length 1024;

  server {
    listen       8080;
    server_name  _;

    # Site root — Showcase as main, index.html for legacy
    root /usr/share/nginx/html;
    index Showcase.html index.html;

    # Make sure Farsi is served correctly
    charset utf-8;

    # Static assets with cache
    location ~* \.(?:css|js|svg|png|jpg|jpeg|gif|ico|webp|woff2?)$ {
      expires 7d;
      add_header Cache-Control "public, max-age=604800, immutable";
      try_files $uri =404;
    }

    # JSON endpoints (e.g., data.json, owners.json)
    location ~* \.json$ {
      add_header Cache-Control "no-cache";
      default_type application/json;
      try_files $uri =404;
    }

    # Pages
    location / {
      try_files $uri $uri/ /index.html;
    }

    error_page 404 /404.html;
  }
}
